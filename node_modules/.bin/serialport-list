#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_serialport@5.0.0@serialport/bin/list.js" "$@"
  ret=$?
else 
  node  "$basedir/../_serialport@5.0.0@serialport/bin/list.js" "$@"
  ret=$?
fi
exit $ret
